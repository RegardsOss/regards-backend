import * as EndpointActions from "./actions"
import EndpointReducer, { getEndpointsItems } from "./reducer"
const EndpointSelectors = {
  getEndpointsItems
}
export { EndpointActions, EndpointReducer, EndpointSelectors }
