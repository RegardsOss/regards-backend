server:
  port: 8888

spring:
  application:
     name: myconfigserver
  profiles:
     active: native
  cloud:
    config:
      server:
        native:
          searchLocations: /home/sbinda/git/rs-access/poc/ConfigServer/src/main/config
          
eureka:
  instance:
    hostname: http://localhost
    nonSecurePort: 9999

my:
  property: myvalue
  otherproperty: default value