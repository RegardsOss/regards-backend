<?xml version="1.0" encoding="UTF-8"?>
<!-- LICENSE_PLACEHOLDER --><project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>fr.cnes.regards.microservices.order</groupId>
	<artifactId>bootstrap-order</artifactId>
	<version>2.0.0-SNAPSHOT</version>

	<parent>
		<groupId>fr.cnes.regards</groupId>
		<artifactId>regards-boot-parent</artifactId>
		<version>2.0.0-SNAPSHOT</version>
		<relativePath></relativePath>
	</parent>

	<properties>
		<fr.cnes.regards.docker.image.name>rs-order</fr.cnes.regards.docker.image.name>
	</properties>

	<url>https://github.com/RegardsOss/RegardsOss.github.io</url>

	<dependencies>
		<!-- Core dependencies -->
		<dependency>
			<groupId>fr.cnes.regards.framework</groupId>
			<artifactId>microservice-core-starter</artifactId>
		</dependency>
		<!-- Module dependencies -->
		<dependency>
			<groupId>fr.cnes.regards.framework</groupId>
			<artifactId>amqp-monitoring-regards-starter</artifactId>
		</dependency>
		<dependency>
			<groupId>fr.cnes.regards.modules.order</groupId>
			<artifactId>order-rest</artifactId>
		</dependency>
	</dependencies>

	<profiles>
		<profile>
			<id>default</id>
			<activation>
				<activeByDefault>true</activeByDefault>
			</activation>
			<properties>
				<!-- Default properties -->
				<filter.name>development</filter.name>
			</properties>
			<build>
				<resources>
					<resource>
						<directory>src/main/resources</directory>
						<filtering>true</filtering>
					</resource>
				</resources>
				<filters>
					<filter>src/main/filters/${filter.name}.properties</filter>
				</filters>
			</build>
		</profile>
		<profile>
			<id>delivery</id>
		</profile>
	</profiles>

	<build>
		<plugins>
			<plugin>
				<groupId>com.spotify</groupId>
				<artifactId>docker-maven-plugin</artifactId>
				<configuration>
					<forceTags>true</forceTags>
					<imageName>${docker.registry.host}/${fr.cnes.regards.docker.image.name}</imageName>
					<baseImage>${docker.registry.host}/rs-alpine-oraclejdk8</baseImage>
					<entryPoint>["./wait-for-it.sh", "rs_config:9031", "-t", "0", "--", "./wait-for-it.sh", "rs_registry:9032", "-t", "0", "--","./wait-for-it.sh", "rs_admin:9033", "-t", "0", "--", "java", "-Xmx1500m", "-Djava.security.egd=file:/dev/./urandom", "-agentlib:jdwp=transport=dt_socket,address=10500,server=y,suspend=n", "-jar", "/${project.build.finalName}.${project.packaging}"]</entryPoint>
					<resources>
						<resource>
							<targetPath>/</targetPath>
							<directory>${project.build.directory}</directory>
							<include>${project.build.finalName}.${project.packaging}</include>
						</resource>
					</resources>
				</configuration>
			</plugin>
		</plugins>
	</build>
</project>
