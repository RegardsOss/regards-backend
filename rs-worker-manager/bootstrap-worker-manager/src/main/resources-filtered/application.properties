# Fixed spring application name
spring.application.name=rs-worker-manager
# Fixed database schema
spring.jpa.properties.hibernate.default_schema=workermanager

# Fixed workspace directory name
microservice.workspace.directory.name=rs_worker_manager

# Default server properties
server.address=0.0.0.0
server.port=9047