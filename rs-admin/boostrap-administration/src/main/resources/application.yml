server:
  adress: localhost
  port: 3333

security:
  client: acme
  secret: acmesecret

spring:
  application:
    name : administration
  hateoas:
    use-hal-as-default-json-media-type : false
  
swagger:
  api:
    name: administration-api

cloud:
  config:
    discovery:
      enabled: false
    server:
      enabled: false
      name: myconfigserver
      
eureka:
  client:
    enabled: false

 
