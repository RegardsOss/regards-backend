#
# LICENSE_PLACEHOLDER
#

# Profiles
spring.profiles.active=dev

# EMBEDDED SERVER CONFIGURATION (ServerProperties)
#server.adress=localhost
#server.port=3333

# JWT
jwt.secret=123456789

# SPRING
spring.application.name=rs-admin
spring.hateoas.use-hal-as-default-json-media-type=false
spring.jackson.deserialization.READ_ENUMS_USING_TO_STRING=true
spring.jackson.serialization.WRITE_ENUMS_USING_TO_STRING=true
spring.cloud.config.uri=http://localhost:8888

# Swagger    
swagger.api.name=administration-api

# Regards parameters
regards.instance.account_acceptance=auto-accept
regards.instance.project.account_acceptance=auto-accept
regards.eureka.client.enabled=true
microservice.dao.enabled=false
microservice.dao.instance.enabled=false
