# Fixed spring application name
spring.application.name=rs-registry
# Fixed database schema
# spring.jpa.properties.hibernate.default_schema=registry
# Default server properties
server.address=@regards.config.cloud.registry.address@
server.port=@regards.config.cloud.registry.port@
