# Fixed spring application name
spring.application.name=rs-dam
# Fixed database schema
spring.jpa.properties.hibernate.default_schema=dam

# Default server properties
server.address=0.0.0.0
server.port=9035

